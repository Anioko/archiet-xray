# AGENT CONTEXT — architecture ground truth for AI coding agents

> Deterministically extracted; regenerate with `xray.py` after structural changes.

- Repo: `microblog` — 34 code files, visibility 78/100
- Modules (top-level): `app`, `migrations`
- Route prefixes: `/edit_profile`, `/explore`, `/export_posts`, `/follow`, `/index`, `/login`, `/logout`, `/messages`, `/notifications`, `/register`, `/reset_password`, `/reset_password_request`, `/search`, `/send_message`, `/tokens`
- Domain entities (5): `Message`, `Notification`, `Post`, `Task`, `User`
- HIGH BLAST RADIUS modules — read dependents before editing: app/api, app/models, app/auth, app/main, app/errors, app/email

## Invariants observed in this codebase (do not regress)
- 21 routes carry an auth guard — new routes should too.
- ⚠️ 6 routes have NO detectable auth guard (e.g. `/login` in `app/auth/routes.py`) — confirm intent before copying the pattern.

---
*Generated by [Archiet X-Ray](https://archiet.com) v0.1.1 — deterministic architecture extraction, no LLM guesses. Unmapped areas are labelled, never invented.*
