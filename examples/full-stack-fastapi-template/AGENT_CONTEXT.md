# AGENT CONTEXT — architecture ground truth for AI coding agents

> Deterministically extracted; regenerate with `xray.py` after structural changes.

- Repo: `full-stack-fastapi-template` — 141 code files, visibility 82/100
- Modules (top-level): `backend`, `frontend`, `hooks`, `scripts`
- Route prefixes: `/health-check`, `/login`, `/me`, `/password-recovery`, `/password-recovery-html-content`, `/reset-password`, `/signup`, `/test-email`, `/users`
- Domain entities (11): `ItemBase`, `ItemsPublic`, `Message`, `NewPassword`, `Token`, `TokenPayload`, `UpdatePassword`, `UserBase`, `UserRegister`, `UserUpdateMe`, `UsersPublic`
- HIGH BLAST RADIUS modules — read dependents before editing: app/core, app/models, tests/utils, app/api, app/utils, app/main

## Invariants observed in this codebase (do not regress)
- 18 routes carry an auth guard — new routes should too.
- ⚠️ token-in-localstorage at `frontend/src/hooks/useAuth.ts`:45 — Auth token written to localStorage/AsyncStorage — prefer httpOnly cookies.
- ⚠️ token-in-localstorage at `frontend/tests/login.spec.ts`:112 — Auth token written to localStorage/AsyncStorage — prefer httpOnly cookies.

---
*Generated by [Archiet X-Ray](https://archiet.com) v0.1.1 — deterministic architecture extraction, no LLM guesses. Unmapped areas are labelled, never invented.*
